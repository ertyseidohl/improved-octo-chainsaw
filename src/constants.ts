// enums

// spawn wave enum
export enum ENEMY_WAVE {
    NONE,
    RANDOM,
    SWOOP,
    BIGV,
    ROWS,
    LAST, // this is only to know max of enumerator
}

export enum COMPONENT_TYPES {
    BASIC_GUN,
    ENGINE,
}
